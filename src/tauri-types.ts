export type ImageInfo = {
  name: string;
  path: string;
  width: number;
  height: number;
  size: number;
};

export type ThumbnailInfo = {
  name: string;
  cache_path: string;
  width: number;
  height: number;
  size: number;
};

