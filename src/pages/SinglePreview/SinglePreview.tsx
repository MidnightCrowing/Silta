// 单图预览页
import './SinglePreview.scss'

export default function SinglePreview({ className }: { className: string }) {
  return (
    <div className={`@container relative ${className}`}>
      123
    </div>
  )
}
