// 单图预览页
import './SinglePreviewPage.scss'

export default function SinglePreviewPage({ className }: { className: string }) {
  return (
    <div className={`@container relative ${className}`}>
      123
    </div>
  )
}
